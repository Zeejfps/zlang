
impl main {

    uses std.array as array;
    uses std.array.Array as Array;
    uses std.list as list;
    uses std.io as io;
    uses std.stdout as stdout;
    uses std.fmt as fmt;
    uses std.string as string;
    uses std.mem.heap as heap;
    
    func main() {
    
        var test_allocator = heap.create_allocator();
        
        var stack_array = array.Array<u8>();
        array.init(&stack_array, 8, &heap.allocator);
        defer array.deinit(&stack_array);
        
        var test_array = array.create<u8>(50, &heap.allocator);
        defer array.destroy(test_array);
        
        var my_list_std = std.list.create<u8>(&heap.allocator);
        var my_list = list.create<u8>(heap);
        
        defer std.list.destroy(my_list);
        // OR
        defer my_list.destroy();
        
        std.list.push_item(my_list, 0);
        // OR
        my_list.push_item(0);
        
        var first_item = std.list.get_item(my_list, 0);
        // OR
        var first_item = get_item(my_list, 0);
        // OR
        var first_item = my_list.get_item(0);
        
        std.io.out.write(std.fmt.to_string(first_item));
        // OR
        std.io.out.write(first_item.to_string());
        // OR
        stdout.write(first_item.to_string());
        
        var str = std.string.create("Some litteral string");
        var str_slice = str.slice(0, 10);
        
        var number: u32 = 10;
        
        struct Vec2<T> = std.vec.Vec2<T>;
    
        stdout.write("Hello world!");
        
        // Testing
        var v1: Vec2<u8> = {
            c1 = 0,
            c2 = 10
        }
        
        var v1: Vec2<u8> = { c1 = 0, c2 = 10 }
        
        // Comments
        var v2: Vec2<u8> = {
            c1 = 2,
            c2 = 3
        }
        s
        var count = 10;
        while (count > 0) {
            count--;
        }
        
        for (var i = 0; i < 10; i++) {
        
        }
        
        var num = 3;
        if (num == 3) {
            
        }
        else if (num == 4) {
        
        }
        else {
            
        }
        
        v1 += v2;
        var v3 = v1 + v2;
    } 
}
