module std.math.vec {

    union Number = u8 | u16 | u32 | u64 | usize | f32

    struct Vec2<T: Number> {
        operator + <T: Number>(v2: ptr<Vec2<T>>);
        operator += <T: Number>(v2: ptr<Vec2<T>>);
    }

    func add<T: Number>(v1: ptr<Vec2<T>>, v2: ptr<Vec2<T>>) -> Vec2<T>;
}